# Contributing

1. Fork and create feature branch.
2. Run `docker compose up --build`.
3. Keep domain rules centralized in `backend/src/domain/stageRules.ts`.
4. Ensure tests pass: `cd backend && npm test`.
5. Submit PR with context and screenshots for UI changes.
